import React from "react";

const App: React.FC = () => {
  return (
    <div
      style={{
        padding: "40px",
        fontSize: "32px",
        fontWeight: "bold",
        fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, sans-serif",
      }}
    >
      Hola Mi Pyme Segura (test)
    </div>
  );
};

export default App;
